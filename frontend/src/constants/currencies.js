/**
 * Default display currency used when the user has not set a preference.
 * EUR is the project default - change here to affect all three call sites.
 */
export const DEFAULT_DISPLAY_CURRENCY = "EUR";
