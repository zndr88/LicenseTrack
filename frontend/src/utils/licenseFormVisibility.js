/** Full create/edit forms expose the input contract independently of read-view preferences. */
export const FULL_LICENSE_FORM_VISIBILITY = Object.freeze({
  supplier: true,
  costCentre: true,
  licenseType: true,
  licenseMetric: true,
  quantity: true,
  quantityPerUnit: true,
  skuCode: true,
  unitPrice: true,
  totalPoPrice: true,
  notes: true,
});
